//TODO let's understand array for in and for of working
/*l
et arr = [22, 33, 44, 55, 66];
console.log(arr);
for (let i of arr) {
    console.log(i);
}
*/
//now let's see how for in works
/*
for (let i in arr) {
    console.log(i);
}
*/
/*
let arrNew = [];
function uniteUnique(arr) {
    console.log(arguments.length);
    for (let i = 0; i < arguments.length; i++) {
        for (let j = 0; j < arguments[i].length; j++) {
            if (arrNew.indexOf(arguments[i][j]) === -1) {
                arrNew.push(arguments[i][j]);
            }
        }
    }
    return arrNew;
}
uniteUnique([1, 3, 2], [5, 2, 1, 4], [2, 1]);

*/

/*
const array = 22;
console.log(Array.isArray(array));
*/

// every element in html elemnt in web app will be object

